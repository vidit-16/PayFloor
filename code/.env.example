# Only `python main.py extract` reads these. Copy to .env, which is gitignored.
OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1   # any OpenAI-compatible endpoint
# ORCHESTRATE_MODEL=gpt-5-mini
# ORCHESTRATE_VISION_MODEL=gpt-5-mini
# ORCHESTRATE_CONCURRENCY=6                   # primary rate-limit control
# ORCHESTRATE_MAX_TOKENS=2000
# ORCHESTRATE_DRY_RUN=1                       # serve from cache only, never call the model
